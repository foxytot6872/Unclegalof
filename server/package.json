{
  "name": "tolopburi-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/prisma-cli.mjs generate && node --watch src/index.js",
    "build": "node -e \"console.log('server build step not required')\"",
    "postinstall": "node scripts/prisma-cli.mjs generate",
    "prisma:generate": "node scripts/prisma-cli.mjs generate",
    "prisma:migrate": "node scripts/prisma-cli.mjs migrate dev",
    "prisma:migrate:deploy": "node scripts/prisma-cli.mjs migrate deploy",
    "prisma:seed": "node prisma/seed.js",
    "prisma:studio": "node scripts/prisma-cli.mjs studio"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "argon2": "^0.44.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^8.3.1",
    "jsonwebtoken": "^9.0.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
